name: Build & Push to GHCR

on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: get repository name as lowercase
        id: image_name
        run: |
          IMAGE="ghcr.io/${{ github.repository }}"
          IMAGE_LOWER=$(echo "$IMAGE" | tr '[:upper:]' '[:lower:]')
          echo "name=$IMAGE_LOWER" >> "$GITHUB_OUTPUT"

      - name: Docker build
        run: |
          docker build -t ghcr.io/${{ steps.image_name.outputs.name }}:latest:latest .

      - name: Docker push
        run: |
          docker push ghcr.io/${{ steps.image_name.outputs.name }}:latest:latest
